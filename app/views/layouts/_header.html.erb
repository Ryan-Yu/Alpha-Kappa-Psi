<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="navbar-inner">
    <div class="container">
      <%= link_to "UC Berkeley Alpha Kappa Psi", '#', id: "logo" %>
      <nav>
        <ul class="nav pull-right">
          <li><%= link_to "Home",            root_path %></li>
          <li><%= link_to "About Us",        about_path %></li>
          
          <!-- Placeholder -->
          <% if active_signed_in? %>
            Signed in as <%= current_active.email %>. Not you?
            <%= link_to "Sign out", destroy_active_session_path, :method => :delete %>
          <% else %>
          <%= link_to "Sign up", new_active_registration_path %> or <%= link_to "sign in", new_active_session_path %>
          <% end %>

        </ul>
      </nav>
    </div>
  </div>
</header>